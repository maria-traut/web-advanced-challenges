info:
  name: Get All Threads
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/threads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
